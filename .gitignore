# Secrets — niemals committen
.env
.env.local
.env.*.local
credentials/*.json
!credentials/*.example.json
!credentials/secrets-vault-map.yaml.example

# n8n CLI exports mit decrypted credentials
*.decrypted.json

# Backups (sollen separat gespeichert werden, nicht im Repo)
backups/
n8n-workflows/backups/

# OS / Editor
.DS_Store
Thumbs.db
.vscode/
!.vscode/settings.json.example
.idea/
*.swp
*.swo

# Node / Tooling
node_modules/
*.log
.cache/

# Drift-Check-Reports (von CI in workdir geschrieben)
drift-report.md

# Claude Code lokal
.claude/settings.local.json
.claude/cache/

# DSGVO: Customer-Identitaet inkl. Kontaktdaten — niemals committen
.claude/customer.json

# Backup-Snapshots vor /template-update (ab v0.6.0)
.template-backup/

# Test-Outputs
tests/output/
*.test.local.json
.claude/settings.local.json.tmp
